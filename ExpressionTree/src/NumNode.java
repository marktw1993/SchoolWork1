package expressionTree;

public class NumNode extends TreeNode
{
	public NumNode(char c)
	{
		super(c);
	}
	public void push(NumNode newNode)
	{
		if(isEmpty())
		{
			et.root = op;
			nodeArray[i++] = op;
		}
	}
}
